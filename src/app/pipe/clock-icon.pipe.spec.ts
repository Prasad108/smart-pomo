import { ClockIconPipe } from './clock-icon.pipe';

describe('ClockIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ClockIconPipe();
    expect(pipe).toBeTruthy();
  });
});
